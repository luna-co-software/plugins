name: Build Plugins

on:
  push:
    tags:
      - 'v*'                    # Full release: v1.0.0 builds all plugins
      - '4k-eq-v*'              # 4K EQ only: 4k-eq-v1.0.0
      - 'multi-comp-v*'         # Multi-Comp only: multi-comp-v1.0.1
      - 'tapemachine-v*'        # TapeMachine only
      - 'tape-echo-v*'          # Tape Echo only
      - 'multi-q-v*'            # Multi-Q only
      - 'convolution-reverb-v*' # Convolution Reverb only
      - 'groovemind-v*'         # GrooveMind only
      - 'duskverb-v*'           # DuskVerb only
      - 'duskamp-v*'            # DuskAmp only
      - 'chord-analyzer-v*'    # Chord Analyzer only
      - 'spectrum-analyzer-v*' # Spectrum Analyzer only
  workflow_dispatch:
    inputs:
      plugin:
        description: 'Plugin to build (leave empty for all)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - '4k-eq'
          - 'multi-comp'
          - 'tapemachine'
          - 'tape-echo'
          - 'multi-q'
          - 'convolution-reverb'
          - 'groovemind'
          - 'duskverb'
          - 'duskamp'
          - 'chord-analyzer'
          - 'spectrum-analyzer'

env:
  BUILD_TYPE: Release

jobs:
  # Determine which plugin(s) to build based on tag
  setup:
    runs-on: ubuntu-22.04
    outputs:
      plugin_target: ${{ steps.parse.outputs.target }}
      plugin_name: ${{ steps.parse.outputs.name }}
      release_name: ${{ steps.parse.outputs.release_name }}
      is_release: ${{ steps.parse.outputs.is_release }}
      plugin_version: ${{ steps.parse.outputs.version }}
    steps:
      - name: Parse tag for plugin selection
        id: parse
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          MANUAL_PLUGIN="${{ github.event.inputs.plugin }}"

          # Default: build all
          TARGET=""
          NAME="All Plugins"
          RELEASE_NAME="DuskAudio-Plugins"
          IS_RELEASE="false"
          VERSION=""

          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            IS_RELEASE="true"
            # Extract version from tag (e.g., "4k-eq-v1.0.5" -> "1.0.5")
            # Only extract if it's a plugin-specific tag, not a bare v* tag
            RAW_VERSION=$(echo "$TAG" | sed 's/.*-v//')
            # Validate it's a clean semver (digits and dots only)
            if [[ "$RAW_VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              VERSION="$RAW_VERSION"
            fi
            echo "Extracted version from tag: ${VERSION:-<default>}"

            if [[ "$TAG" == 4k-eq-v* ]]; then
              TARGET="FourKEQ_All"
              NAME="4K EQ"
              RELEASE_NAME="4k-eq"
            elif [[ "$TAG" == multi-comp-v* ]]; then
              TARGET="MultiComp_All"
              NAME="Multi-Comp"
              RELEASE_NAME="multi-comp"
            elif [[ "$TAG" == tapemachine-v* ]]; then
              TARGET="TapeMachine_All"
              NAME="TapeMachine"
              RELEASE_NAME="tapemachine"
            elif [[ "$TAG" == multi-q-v* ]]; then
              TARGET="MultiQ_All"
              NAME="Multi-Q"
              RELEASE_NAME="multi-q"
            elif [[ "$TAG" == tape-echo-v* ]]; then
              TARGET="TapeEcho_All"
              NAME="Tape Echo"
              RELEASE_NAME="tape-echo"
            elif [[ "$TAG" == convolution-reverb-v* ]]; then
              TARGET="ConvolutionReverb_All"
              NAME="Convolution Reverb"
              RELEASE_NAME="convolution-reverb"
            elif [[ "$TAG" == groovemind-v* ]]; then
              TARGET="GrooveMind_All"
              NAME="GrooveMind"
              RELEASE_NAME="groovemind"
            elif [[ "$TAG" == duskverb-v* ]]; then
              TARGET="DuskVerb_All"
              NAME="DuskVerb"
              RELEASE_NAME="duskverb"
            elif [[ "$TAG" == duskamp-v* ]]; then
              TARGET="DuskAmp_All"
              NAME="DuskAmp"
              RELEASE_NAME="duskamp"
            elif [[ "$TAG" == chord-analyzer-v* ]]; then
              TARGET="ChordAnalyzer_All ChordAnalyzerMIDI_All ChordAnalyzerHeadless_LV2"
              NAME="Chord Analyzer"
              RELEASE_NAME="chord-analyzer"
            elif [[ "$TAG" == spectrum-analyzer-v* ]]; then
              TARGET="SpectrumAnalyzer_All"
              NAME="Spectrum Analyzer"
              RELEASE_NAME="spectrum-analyzer"
            fi
          elif [[ -n "$MANUAL_PLUGIN" ]]; then
            case "$MANUAL_PLUGIN" in
              4k-eq) TARGET="FourKEQ_All"; NAME="4K EQ"; RELEASE_NAME="4k-eq" ;;
              multi-comp) TARGET="MultiComp_All"; NAME="Multi-Comp"; RELEASE_NAME="multi-comp" ;;
              tapemachine) TARGET="TapeMachine_All"; NAME="TapeMachine"; RELEASE_NAME="tapemachine" ;;
              tape-echo) TARGET="TapeEcho_All"; NAME="Tape Echo"; RELEASE_NAME="tape-echo" ;;
              multi-q) TARGET="MultiQ_All"; NAME="Multi-Q"; RELEASE_NAME="multi-q" ;;
              convolution-reverb) TARGET="ConvolutionReverb_All"; NAME="Convolution Reverb"; RELEASE_NAME="convolution-reverb" ;;
              groovemind) TARGET="GrooveMind_All"; NAME="GrooveMind"; RELEASE_NAME="groovemind" ;;
              duskverb) TARGET="DuskVerb_All"; NAME="DuskVerb"; RELEASE_NAME="duskverb" ;;
              duskamp) TARGET="DuskAmp_All"; NAME="DuskAmp"; RELEASE_NAME="duskamp" ;;
              chord-analyzer) TARGET="ChordAnalyzer_All ChordAnalyzerMIDI_All ChordAnalyzerHeadless_LV2"; NAME="Chord Analyzer"; RELEASE_NAME="chord-analyzer" ;;
              spectrum-analyzer) TARGET="SpectrumAnalyzer_All"; NAME="Spectrum Analyzer"; RELEASE_NAME="spectrum-analyzer" ;;
            esac
          fi

          echo "target=$TARGET" >> $GITHUB_OUTPUT
          echo "name=$NAME" >> $GITHUB_OUTPUT
          echo "release_name=$RELEASE_NAME" >> $GITHUB_OUTPUT
          echo "is_release=$IS_RELEASE" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building: $NAME (target: ${TARGET:-all}) version: ${VERSION:-default}"

      # Refresh plugins/shared/PatreonBackers.h once from the live Patreon campaign and hand it to
      # every build job as an artifact, so releases ship the current supporters list without anyone
      # hand-editing the header. Runs here (ubuntu-latest has python3) rather than inside each build
      # container, and only once so the token refresh isn't raced by concurrent jobs.
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false  # this job never pushes; don't leave the token in git config

      - name: Refresh Patreon supporters
        # Best-effort: with no secrets (forks / dev builds) the committed list is kept. When the
        # secrets ARE configured this must succeed — a release should never silently ship a stale
        # list (see scripts/update-patrons.py for the one-time token setup).
        shell: bash
        env:
          PATREON_ACCESS_TOKEN: ${{ secrets.PATREON_ACCESS_TOKEN }}
          PATREON_REFRESH_TOKEN: ${{ secrets.PATREON_REFRESH_TOKEN }}
          PATREON_CLIENT_ID: ${{ secrets.PATREON_CLIENT_ID }}
          PATREON_CLIENT_SECRET: ${{ secrets.PATREON_CLIENT_SECRET }}
        run: |
          present=0
          for v in "${PATREON_ACCESS_TOKEN}" "${PATREON_REFRESH_TOKEN}" "${PATREON_CLIENT_ID}" "${PATREON_CLIENT_SECRET}"; do
            [ -n "$v" ] && present=$((present + 1))
          done
          if [ "$present" -eq 0 ]; then
            echo "::warning::Patreon secrets not configured - using the committed supporters list (may be stale)."
            exit 0
          fi
          if [ "$present" -lt 4 ]; then
            echo "::error::Patreon secrets are only partially configured ($present/4). Set ALL of PATREON_ACCESS_TOKEN, PATREON_REFRESH_TOKEN, PATREON_CLIENT_ID and PATREON_CLIENT_SECRET, or none."
            exit 1
          fi
          umask 077
          mkdir -p "${HOME}/.config/dusk-audio"
          cfg="${HOME}/.config/dusk-audio/patreon.json"
          # Always remove the credential file on exit, even if the refresh fails.
          trap 'rm -f "${cfg}"' EXIT
          # json.dump escapes special characters in the secrets; a raw heredoc would corrupt the
          # JSON on a quote/backslash in a token.
          python3 -c 'import json, os, sys; json.dump({"access_token": os.environ["PATREON_ACCESS_TOKEN"], "refresh_token": os.environ["PATREON_REFRESH_TOKEN"], "client_id": os.environ["PATREON_CLIENT_ID"], "client_secret": os.environ["PATREON_CLIENT_SECRET"]}, open(sys.argv[1], "w"))' "${cfg}"
          if ! python3 scripts/update-patrons.py; then
            echo "::error::Patreon refresh failed despite configured secrets - refusing to ship a stale supporters list. Check the token/API and re-run."
            exit 1
          fi

      - name: Upload refreshed supporters header
        uses: actions/upload-artifact@v4
        with:
          name: patreon-header
          path: plugins/shared/PatreonBackers.h
          retention-days: 1

  build-linux:
    needs: setup
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    container:
      image: ubuntu:20.04  # Use Ubuntu 20.04 for glibc 2.31 (widest compatibility)
    steps:
      - name: Install git and dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Inline because checkout cannot run until git is installed and the
          # shared script is not on disk yet. Stock Ubuntu repositories only.
          # The timeout guards stalls, retries guard hard failures, and the
          # dpkg repair handles interrupted transactions before the next try.
          bootstrap() {
            timeout 60 apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=30 && \
            timeout 120 apt-get install -y --no-install-recommends -o Acquire::Retries=3 \
              git ca-certificates
          }
          for i in 1 2; do
            bootstrap && break
            if [ "$i" = "2" ]; then
              echo "::error::apt bootstrap unavailable after 2 attempts"
              exit 1
            fi
            echo "::warning::bootstrap attempt $i failed or timed out; retrying in 5s"
            timeout 45 dpkg --configure -a || true
            sleep 5
          done

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false  # build job never pushes; don't leave the token in git config

      - name: Install Linux apt dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
          # Worst case, counting the 45s dpkg repair that precedes each retry:
          # bootstrap 2x(60+120)+45+5 = 6.8m; plain packages 2x(45+90)+45+5
          # = 5.3m; PPA retry = 2x60+5 = 2.1m; post-source update 2x45+45+5
          # = 2.3m; final packages = 5.3m. Total apt ceiling is 21.9m,
          # leaving 8m in this 30m job for checkout, configuration, and
          # compilation.
          APT_ATTEMPTS: 2
          APT_UPDATE_TIMEOUT: 45
          APT_INSTALL_TIMEOUT: 90
          APT_RETRY_SLEEP: 5
        run: |
          # This job builds inside an Ubuntu 20.04 (focal) container for glibc
          # compat, where gcc-11 is NOT in the base repos — it needs the
          # ubuntu-toolchain-r/test PPA. That PPA add is occasionally flaky
          # (launchpad resolution stalls), which failed past release builds, so
          # the PPA command is the only apt operation it cannot own.
          .github/scripts/apt_install.sh software-properties-common wget gpg
          for i in 1 2; do
            timeout 60 add-apt-repository -y ppa:ubuntu-toolchain-r/test && break
            [ "$i" = "2" ] && exit 1
            echo "PPA attempt 1 failed; retrying in 5s"
            sleep 5
          done
          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ focal main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
          .github/scripts/apt_install.sh --update-only
          .github/scripts/apt_install.sh \
            gcc-11 g++-11 cmake build-essential pkg-config \
            libasound2-dev libjack-jackd2-dev ladspa-sdk \
            libcurl4-openssl-dev libfreetype6-dev libx11-dev xvfb \
            libxcomposite-dev libxcursor-dev libxext-dev libxinerama-dev \
            libxrandr-dev libxrender-dev libwebkit2gtk-4.0-dev \
            libglu1-mesa-dev mesa-common-dev
          # Set GCC 11 as default for all compiler symlinks
          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 100
          update-alternatives --install /usr/bin/cc cc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/c++ c++ /usr/bin/g++-11 100
          gcc --version
          g++ --version
          cmake --version

      - name: Apply refreshed supporters list
        # Always use the header refreshed once in the setup job. download-artifact errors if the
        # artifact is missing, failing the build loudly rather than silently shipping a possibly
        # stale committed list (the setup job always uploads it, so a miss is a real error).
        uses: actions/download-artifact@v4
        with:
          name: patreon-header
          path: patreon-header

      - name: Install refreshed supporters header
        shell: bash
        run: |
          cp patreon-header/PatreonBackers.h plugins/shared/PatreonBackers.h
          echo "Applied refreshed PatreonBackers.h from the setup job"

      - name: Checkout JUCE
        uses: actions/checkout@v4
        with:
          repository: juce-framework/JUCE
          path: JUCE
          ref: 8.0.4  # Use JUCE 8.x for modern C++ features

      - name: Configure CMake
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DDUSK_COPY_AFTER_BUILD=OFF \
            -DJUCE_PATH=$(pwd)/JUCE

      - name: Build plugins
        # ubuntu:20.04 container's default /bin/sh is dash, which doesn't
        # support [[ ]]. Force bash so the CLAP-target conditional works.
        shell: bash
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          TARGET="${{ needs.setup.outputs.plugin_target }}"
          if [ -n "$TARGET" ]; then
            echo "Building specific target(s): $TARGET"
            for t in $TARGET; do
              cmake --build build --config ${{ env.BUILD_TYPE }} --target "$t" -j$(nproc)
              # Also build matching CLAP target (FourKEQ_All -> FourKEQ_CLAP).
              # CLAP targets are not part of the JUCE-generated _All aggregate.
              # Skip *_LV2 targets (no CLAP form). Skip if CLAP target doesn't exist.
              if [[ "$t" == *_All ]]; then
                CLAP_TARGET="${t%_All}_CLAP"
                echo "Building CLAP target: $CLAP_TARGET"
                cmake --build build --config ${{ env.BUILD_TYPE }} --target "$CLAP_TARGET" -j$(nproc) || \
                  echo "CLAP target $CLAP_TARGET not found, skipping"
              fi
            done
          else
            echo "Building all plugins"
            cmake --build build --config ${{ env.BUILD_TYPE }} -j$(nproc)
          fi

      - name: DuskVerb regression tests
        # Only when DuskVerb is in scope: a bare/all build (empty target) or a
        # target list that includes DuskVerb. The build dir is configured for the
        # whole project, so the test target is always available to build here.
        if: ${{ needs.setup.outputs.plugin_target == '' || contains(needs.setup.outputs.plugin_target, 'DuskVerb') }}
        shell: bash
        run: |
          cmake --build build --config ${{ env.BUILD_TYPE }} \
            --target DuskVerbStereoImageTest DuskVerbNyquistClampTest DuskVerbEditorResizeTest -j"$(nproc)"
          # DuskVerbEditorResizeTest is a JUCE application that constructs the
          # real editor, so it needs an X display; the runner has none, hence
          # xvfb-run. The DSP tests do not care either way.
          xvfb-run -a --server-args="-screen 0 1280x1024x24" \
            ctest --test-dir build -R '^DuskVerb' --output-on-failure --no-tests=error

      - name: Validate plugins
        shell: bash
        run: |
          echo "=== Validating Linux plugin builds ==="
          found_vst3=0
          found_lv2=0

          # Search build/lib, build/bin, and build/plugins (artefacts directories) for plugin outputs
          # Different plugins may output to different locations depending on CMakeLists.txt configuration
          while IFS= read -r -d '' vst3; do
            # Skip if no binary exists
            if ! ls "$vst3/Contents/x86_64-linux/"*.so >/dev/null 2>&1; then
              echo "⊘ Skipping (no binary): $vst3"
              continue
            fi
            echo "✓ Found: $vst3"
            found_vst3=1
            echo "  ✓ Binary exists"
          done < <(find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null)

          while IFS= read -r -d '' lv2; do
            # Skip if no binary exists
            if ! ls "$lv2/"*.so >/dev/null 2>&1; then
              echo "⊘ Skipping (no binary): $lv2"
              continue
            fi
            echo "✓ Found: $lv2"
            found_lv2=1
            if [ -f "$lv2/manifest.ttl" ]; then
              echo "  ✓ Binary and manifest exist"
            else
              echo "  ⚠ Missing manifest.ttl"
            fi
          done < <(find build/lib build/bin build/plugins -name "*.lv2" -type d -print0 2>/dev/null)

          # Fail if no plugins were found
          if [ $found_vst3 -eq 0 ]; then
            echo "✗ No VST3 plugins found!"
            exit 1
          fi

          if [ $found_lv2 -eq 0 ]; then
            echo "✗ No LV2 plugins found!"
            exit 1
          fi

          echo "=== Validation passed ==="

      - name: Prepare artifacts
        shell: bash
        run: |
          mkdir -p artifacts/linux/VST3 artifacts/linux/LV2

          # Copy VST3 plugins from build/lib/, build/bin/, and build/plugins/ (handles spaces in names)
          find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null | while IFS= read -r -d '' vst3; do
            if ls "$vst3/Contents/x86_64-linux/"*.so >/dev/null 2>&1; then
              cp -r "$vst3" artifacts/linux/VST3/
            fi
          done

          # Copy LV2 plugins from build/lib/, build/bin/, and build/plugins/ (handles spaces in names)
          find build/lib build/bin build/plugins -name "*.lv2" -type d -print0 2>/dev/null | while IFS= read -r -d '' lv2; do
            if ls "$lv2/"*.so >/dev/null 2>&1; then
              cp -r "$lv2" artifacts/linux/LV2/
            fi
          done

          # Copy CLAP plugins (single .clap shared library on Linux). clap-juce-extensions
          # writes to build/<Plugin>_artefacts/Release/CLAP/, outside the lib/bin/plugins paths.
          mkdir -p artifacts/linux/CLAP
          find build -name "*.clap" -type f -print0 2>/dev/null | while IFS= read -r -d '' clap; do
            cp "$clap" artifacts/linux/CLAP/
          done

          # List what we collected
          echo "=== Linux Artifacts ==="
          find artifacts/linux -type f \( -name "*.so" -o -name "*.clap" \) | head -20

      - name: Upload Linux artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugins-linux
          path: artifacts/linux/
          retention-days: 30

  build-linux-arm64:
    needs: setup
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    container:
      image: ubuntu:22.04  # glibc 2.35 (Debian bookworm uses 2.36, so this is compatible)
    steps:
      - name: Install git and dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Inline because checkout cannot run until git is installed and the
          # shared script is not on disk yet. Stock Ubuntu repositories only.
          # The timeout guards stalls, retries guard hard failures, and the
          # dpkg repair handles interrupted transactions before the next try.
          bootstrap() {
            timeout 60 apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=30 && \
            timeout 120 apt-get install -y --no-install-recommends -o Acquire::Retries=3 \
              git ca-certificates
          }
          for i in 1 2; do
            bootstrap && break
            if [ "$i" = "2" ]; then
              echo "::error::apt bootstrap unavailable after 2 attempts"
              exit 1
            fi
            echo "::warning::bootstrap attempt $i failed or timed out; retrying in 5s"
            timeout 45 dpkg --configure -a || true
            sleep 5
          done

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          persist-credentials: false  # build job never pushes; don't leave the token in git config

      - name: Install Linux apt dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
          # Worst case, counting the 45s dpkg repair that precedes each retry:
          # bootstrap 2x(60+120)+45+5 = 6.8m; plain packages 2x(45+90)+45+5
          # = 5.3m; post-source update 2x45+45+5 = 2.3m; final packages =
          # 5.3m. Total apt ceiling is 19.8m, leaving
          # over 13m in this 30m job for checkout, configuration, and build.
          APT_ATTEMPTS: 2
          APT_UPDATE_TIMEOUT: 45
          APT_INSTALL_TIMEOUT: 90
          APT_RETRY_SLEEP: 5
        run: |
          # Ubuntu 22.04 ships GCC 11 natively — no PPA needed.
          .github/scripts/apt_install.sh wget gpg
          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
          .github/scripts/apt_install.sh --update-only
          .github/scripts/apt_install.sh \
            gcc-11 g++-11 cmake build-essential pkg-config \
            libasound2-dev libjack-jackd2-dev ladspa-sdk \
            libcurl4-openssl-dev libfreetype6-dev libx11-dev \
            libxcomposite-dev libxcursor-dev libxext-dev libxinerama-dev \
            libxrandr-dev libxrender-dev libwebkit2gtk-4.0-dev \
            libglu1-mesa-dev mesa-common-dev
          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 100
          update-alternatives --install /usr/bin/cc cc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/c++ c++ /usr/bin/g++-11 100
          gcc --version
          g++ --version
          cmake --version

      - name: Apply refreshed supporters list
        # Always use the header refreshed once in the setup job. download-artifact errors if the
        # artifact is missing, failing the build loudly rather than silently shipping a possibly
        # stale committed list (the setup job always uploads it, so a miss is a real error).
        uses: actions/download-artifact@v4
        with:
          name: patreon-header
          path: patreon-header

      - name: Install refreshed supporters header
        shell: bash
        run: |
          cp patreon-header/PatreonBackers.h plugins/shared/PatreonBackers.h
          echo "Applied refreshed PatreonBackers.h from the setup job"

      - name: Checkout JUCE
        uses: actions/checkout@v4
        with:
          repository: juce-framework/JUCE
          path: JUCE
          ref: 8.0.4  # Use JUCE 8.x for modern C++ features

      - name: Configure CMake
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DDUSK_COPY_AFTER_BUILD=OFF \
            -DJUCE_PATH=$(pwd)/JUCE

      - name: Build plugins
        # ubuntu:22.04 container's default /bin/sh is dash, which doesn't
        # support [[ ]]. Force bash so the CLAP-target conditional works.
        shell: bash
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          TARGET="${{ needs.setup.outputs.plugin_target }}"
          if [ -n "$TARGET" ]; then
            echo "Building specific target(s): $TARGET"
            for t in $TARGET; do
              cmake --build build --config ${{ env.BUILD_TYPE }} --target "$t" -j$(nproc)
              # Also build matching CLAP target (FourKEQ_All -> FourKEQ_CLAP).
              # CLAP targets are not part of the JUCE-generated _All aggregate.
              # Skip *_LV2 targets (no CLAP form). Skip if CLAP target doesn't exist.
              if [[ "$t" == *_All ]]; then
                CLAP_TARGET="${t%_All}_CLAP"
                echo "Building CLAP target: $CLAP_TARGET"
                cmake --build build --config ${{ env.BUILD_TYPE }} --target "$CLAP_TARGET" -j$(nproc) || \
                  echo "CLAP target $CLAP_TARGET not found, skipping"
              fi
            done
          else
            echo "Building all plugins"
            cmake --build build --config ${{ env.BUILD_TYPE }} -j$(nproc)
          fi

      - name: Validate plugins
        shell: bash
        run: |
          echo "=== Validating Linux ARM64 plugin builds ==="
          found_vst3=0
          found_lv2=0

          while IFS= read -r -d '' vst3; do
            if ! ls "$vst3/Contents/aarch64-linux/"*.so >/dev/null 2>&1; then
              echo "⊘ Skipping (no binary): $vst3"
              continue
            fi
            echo "✓ Found: $vst3"
            found_vst3=1
            echo "  ✓ Binary exists"
          done < <(find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null)

          while IFS= read -r -d '' lv2; do
            if ! ls "$lv2/"*.so >/dev/null 2>&1; then
              echo "⊘ Skipping (no binary): $lv2"
              continue
            fi
            echo "✓ Found: $lv2"
            found_lv2=1
            if [ -f "$lv2/manifest.ttl" ]; then
              echo "  ✓ Binary and manifest exist"
            else
              echo "  ⚠ Missing manifest.ttl"
            fi
          done < <(find build/lib build/bin build/plugins -name "*.lv2" -type d -print0 2>/dev/null)

          if [ $found_vst3 -eq 0 ]; then
            echo "✗ No VST3 plugins found!"
            exit 1
          fi

          if [ $found_lv2 -eq 0 ]; then
            echo "✗ No LV2 plugins found!"
            exit 1
          fi

          echo "=== Validation passed ==="

      - name: Prepare artifacts
        shell: bash
        run: |
          mkdir -p artifacts/linux-arm64/VST3 artifacts/linux-arm64/LV2 artifacts/linux-arm64/CLAP

          find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null | while IFS= read -r -d '' vst3; do
            if ls "$vst3/Contents/aarch64-linux/"*.so >/dev/null 2>&1; then
              cp -r "$vst3" artifacts/linux-arm64/VST3/
            fi
          done

          find build/lib build/bin build/plugins -name "*.lv2" -type d -print0 2>/dev/null | while IFS= read -r -d '' lv2; do
            if ls "$lv2/"*.so >/dev/null 2>&1; then
              cp -r "$lv2" artifacts/linux-arm64/LV2/
            fi
          done

          # Copy CLAP plugins (single .clap shared library on Linux). clap-juce-extensions
          # writes to build/<Plugin>_artefacts/Release/CLAP/, outside the lib/bin/plugins paths.
          find build -name "*.clap" -type f -print0 2>/dev/null | while IFS= read -r -d '' clap; do
            cp "$clap" artifacts/linux-arm64/CLAP/
          done

          echo "=== Linux ARM64 Artifacts ==="
          find artifacts/linux-arm64 -type f \( -name "*.so" -o -name "*.clap" \) | head -20

      - name: Upload Linux ARM64 artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugins-linux-arm64
          path: artifacts/linux-arm64/
          retention-days: 30

  build-windows:
    needs: setup
    runs-on: windows-2022
    # 60 min: MSVC link is slow on the windows-2022 runner. Standalone
    # builds were dropped from all plugins (commit context) which should
    # bring most plugins under 30 min, but keeping the wider budget as
    # headroom for future format additions (CLAP, etc.).
    timeout-minutes: 60
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Apply refreshed supporters list
        # Always use the header refreshed once in the setup job. download-artifact errors if the
        # artifact is missing, failing the build loudly rather than silently shipping a possibly
        # stale committed list (the setup job always uploads it, so a miss is a real error).
        uses: actions/download-artifact@v4
        with:
          name: patreon-header
          path: patreon-header

      - name: Install refreshed supporters header
        shell: bash
        run: |
          cp patreon-header/PatreonBackers.h plugins/shared/PatreonBackers.h
          echo "Applied refreshed PatreonBackers.h from the setup job"

      - name: Checkout JUCE
        uses: actions/checkout@v4
        with:
          repository: juce-framework/JUCE
          path: JUCE
          ref: 8.0.4  # Use JUCE 8.x for modern C++ features

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Configure CMake
        run: |
          if ("${{ needs.setup.outputs.plugin_version }}") {
            $env:PLUGIN_VERSION_OVERRIDE = "${{ needs.setup.outputs.plugin_version }}"
          }
          cmake -B build -G Ninja `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} `
            -DDUSK_COPY_AFTER_BUILD=OFF `
            -DBUILD_LV2=OFF `
            -DJUCE_PATH=${{ github.workspace }}/JUCE

      - name: Build plugins
        run: |
          if ("${{ needs.setup.outputs.plugin_version }}") {
            $env:PLUGIN_VERSION_OVERRIDE = "${{ needs.setup.outputs.plugin_version }}"
          }
          $target = "${{ needs.setup.outputs.plugin_target }}"
          if ($target) {
            Write-Host "Building specific target(s): $target"
            foreach ($t in $target.Split(' ')) {
              # Windows configures with -DBUILD_LV2=OFF, so any *_LV2 targets
              # (including the hand-rolled ChordAnalyzerHeadless_LV2) don't
              # exist in the generated build files. Skip them silently rather
              # than letting ninja fail with "unknown target".
              if ($t -like "*_LV2") {
                Write-Host "Skipping $t (LV2 disabled on Windows)"
                continue
              }
              cmake --build build --target $t -j $env:NUMBER_OF_PROCESSORS
              if ($LASTEXITCODE -ne 0) {
                Write-Error "Build failed for target $t (exit code $LASTEXITCODE)"
                exit $LASTEXITCODE
              }
              # Also build matching CLAP target (FourKEQ_All -> FourKEQ_CLAP).
              # CLAP targets are not part of the JUCE-generated _All aggregate.
              # Unlike the main target above, a missing CLAP target is OK
              # (e.g. when the submodule is absent) — reset $LASTEXITCODE
              # so the step doesn't fail solely on missing CLAP.
              if ($t -like "*_All") {
                $clapTarget = $t -replace '_All$', '_CLAP'
                Write-Host "Building CLAP target: $clapTarget"
                cmake --build build --target $clapTarget -j $env:NUMBER_OF_PROCESSORS
                if ($LASTEXITCODE -ne 0) {
                  Write-Host "CLAP target $clapTarget not found, skipping"
                  $LASTEXITCODE = 0
                }
              }
            }
          } else {
            Write-Host "Building all plugins"
            cmake --build build -j $env:NUMBER_OF_PROCESSORS
          }

      - name: Validate plugins
        shell: pwsh
        run: |
          Write-Host "=== Validating Windows plugin builds ==="
          $foundVst3 = $false

          # Check VST3 plugins in build/lib/, build/bin/, and build/plugins/ (artefacts directories)
          $searchPaths = @("build/lib", "build/bin", "build/plugins")
          foreach ($searchPath in $searchPaths) {
            $vst3s = Get-ChildItem -Path $searchPath -Recurse -Filter "*.vst3" -Directory -ErrorAction SilentlyContinue
            foreach ($vst3 in $vst3s) {
              # Check if the plugin has a .vst3 binary file inside
              $binaryFile = Get-ChildItem -Path $vst3.FullName -Recurse -Filter "*.vst3" -File -ErrorAction SilentlyContinue
              if (-not $binaryFile) {
                Write-Host "⊘ Skipping (no binary): $($vst3.FullName)"
                continue
              }
              Write-Host "✓ Found: $($vst3.FullName)"
              Write-Host "  ✓ Binary exists: $($binaryFile.FullName)"
              $foundVst3 = $true
            }
          }

          if (-not $foundVst3) {
            Write-Host "✗ No VST3 plugins with binaries found!"
            Write-Host "=== Debug: Searching entire build directory ==="
            Get-ChildItem -Path build -Recurse -Filter "*.vst3" | ForEach-Object { Write-Host $_.FullName }
            exit 1
          }

          Write-Host "=== Validation passed ==="

      - name: Prepare artifacts
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts/windows/VST3

          # Copy VST3 plugins from build/lib/, build/bin/, and build/plugins/ that have actual binaries
          $searchPaths = @("build/lib", "build/bin", "build/plugins")
          foreach ($searchPath in $searchPaths) {
            $vst3s = Get-ChildItem -Path $searchPath -Recurse -Filter "*.vst3" -Directory -ErrorAction SilentlyContinue
            foreach ($vst3 in $vst3s) {
              # Check if the plugin has a .vst3 binary file inside
              $binaryFile = Get-ChildItem -Path $vst3.FullName -Recurse -Filter "*.vst3" -File -ErrorAction SilentlyContinue
              if ($binaryFile) {
                Write-Host "Copying: $($vst3.FullName)"
                Copy-Item -Path $vst3.FullName -Destination "artifacts/windows/VST3/" -Recurse
              } else {
                Write-Host "Skipping (no binary): $($vst3.FullName)"
              }
            }
          }

          # Copy CLAP plugins (single .clap DLL on Windows)
          New-Item -ItemType Directory -Force -Path artifacts/windows/CLAP
          $claps = Get-ChildItem -Path build -Recurse -Filter "*.clap" -File -ErrorAction SilentlyContinue
          foreach ($clap in $claps) {
            Write-Host "Copying CLAP: $($clap.FullName)"
            Copy-Item -Path $clap.FullName -Destination "artifacts/windows/CLAP/"
          }

          Write-Host "=== Windows Artifacts ==="
          Get-ChildItem -Path artifacts/windows -Recurse -Include "*.vst3","*.clap" | Select-Object FullName

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugins-windows
          path: artifacts/windows/
          retention-days: 30

  build-macos:
    needs: setup
    runs-on: macos-14  # ARM64 runner (M1)
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Apply refreshed supporters list
        # Always use the header refreshed once in the setup job. download-artifact errors if the
        # artifact is missing, failing the build loudly rather than silently shipping a possibly
        # stale committed list (the setup job always uploads it, so a miss is a real error).
        uses: actions/download-artifact@v4
        with:
          name: patreon-header
          path: patreon-header

      - name: Install refreshed supporters header
        shell: bash
        run: |
          cp patreon-header/PatreonBackers.h plugins/shared/PatreonBackers.h
          echo "Applied refreshed PatreonBackers.h from the setup job"

      - name: Checkout JUCE
        uses: actions/checkout@v4
        with:
          repository: juce-framework/JUCE
          path: JUCE
          ref: 8.0.4  # Use JUCE 8.x for modern C++ features

      - name: Configure CMake (Universal Binary)
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          cmake -B build \
            -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }} \
            -DDUSK_COPY_AFTER_BUILD=OFF \
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="10.15" \
            -DJUCE_PATH=${{ github.workspace }}/JUCE

      - name: Build plugins
        run: |
          if [ -n "${{ needs.setup.outputs.plugin_version }}" ]; then
            export PLUGIN_VERSION_OVERRIDE="${{ needs.setup.outputs.plugin_version }}"
          fi
          TARGET="${{ needs.setup.outputs.plugin_target }}"
          if [ -n "$TARGET" ]; then
            echo "Building specific target(s): $TARGET"
            for t in $TARGET; do
              cmake --build build --config ${{ env.BUILD_TYPE }} --target "$t" -j$(sysctl -n hw.ncpu)
              # Also build matching CLAP target (FourKEQ_All -> FourKEQ_CLAP).
              # CLAP targets are not part of the JUCE-generated _All aggregate.
              if [[ "$t" == *_All ]]; then
                CLAP_TARGET="${t%_All}_CLAP"
                echo "Building CLAP target: $CLAP_TARGET"
                cmake --build build --config ${{ env.BUILD_TYPE }} --target "$CLAP_TARGET" -j$(sysctl -n hw.ncpu) || \
                  echo "CLAP target $CLAP_TARGET not found, skipping"
              fi
            done
          else
            echo "Building all plugins"
            cmake --build build --config ${{ env.BUILD_TYPE }} -j$(sysctl -n hw.ncpu)
          fi

      - name: Validate plugins
        run: |
          echo "=== Validating macOS plugin builds ==="
          found_vst3=0
          found_au=0

          # Search build/lib, build/bin, and build/plugins (artefacts directories) for plugin outputs
          while IFS= read -r -d '' vst3; do
            # Verify binary exists - look for the binary inside Contents/MacOS
            plugin_name=$(basename "$vst3" .vst3)
            binary="$vst3/Contents/MacOS/$plugin_name"
            if [ ! -f "$binary" ]; then
              echo "⊘ Skipping (no binary): $vst3"
              continue
            fi
            echo "✓ Found: $vst3"
            found_vst3=1
            archs=$(lipo -info "$binary" 2>/dev/null || file "$binary")
            echo "  Architectures: $archs"
            if echo "$archs" | grep -q "arm64" && echo "$archs" | grep -q "x86_64"; then
              echo "  ✓ Universal binary confirmed"
            else
              echo "  ⚠ May not be universal binary"
            fi
          done < <(find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null)

          # Check AU plugins
          while IFS= read -r -d '' au; do
            plugin_name=$(basename "$au" .component)
            if [ ! -f "$au/Contents/MacOS/$plugin_name" ]; then
              echo "⊘ Skipping AU (no binary): $au"
              continue
            fi
            echo "✓ Found AU: $au"
            found_au=1
          done < <(find build/lib build/bin build/plugins -name "*.component" -type d -print0 2>/dev/null)

          # Fail if no plugins were found
          if [ $found_vst3 -eq 0 ]; then
            echo "✗ No VST3 plugins found!"
            exit 1
          fi

          echo "=== Validation passed ==="

      - name: Import Code Signing Certificate
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' }}
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          # Create temporary keychain
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # Import certificate
          echo $MACOS_CERTIFICATE | base64 --decode > certificate.p12
          security import certificate.p12 -P "$MACOS_CERTIFICATE_PWD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          rm certificate.p12

      - name: Code Sign Plugins
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' }}
        env:
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        run: |
          echo "=== Code Signing macOS Plugins ==="

          # Sign VST3 plugins
          for plugin in build/*_artefacts/Release/VST3/*.vst3; do
            if [ -d "$plugin" ]; then
              echo "Signing: $plugin"
              codesign --force --deep --sign "$MACOS_SIGNING_IDENTITY" \
                --options runtime \
                --timestamp \
                "$plugin"

              # Verify signature
              codesign --verify --verbose "$plugin"
            fi
          done

          # Sign AU plugins
          for plugin in build/*_artefacts/Release/AU/*.component; do
            if [ -d "$plugin" ]; then
              echo "Signing: $plugin"
              codesign --force --deep --sign "$MACOS_SIGNING_IDENTITY" \
                --options runtime \
                --timestamp \
                "$plugin"

              codesign --verify --verbose "$plugin"
            fi
          done

          # Sign CLAP plugins (bundle directory on macOS)
          for plugin in build/*_artefacts/Release/CLAP/*.clap; do
            if [ -d "$plugin" ]; then
              echo "Signing: $plugin"
              codesign --force --deep --sign "$MACOS_SIGNING_IDENTITY" \
                --options runtime \
                --timestamp \
                "$plugin"

              codesign --verify --verbose "$plugin"
            fi
          done

      - name: Notarize Plugins
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' && startsWith(github.ref, 'refs/tags/v') }}
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          echo "=== Notarizing macOS Plugins ==="

          # Create zip for notarization
          cd build

          # Collect all signed plugins
          mkdir -p notarize_staging
          find . -path "*Release/VST3/*.vst3" -type d -exec cp -r {} notarize_staging/ \;
          find . -path "*Release/AU/*.component" -type d -exec cp -r {} notarize_staging/ \;
          find . -path "*Release/CLAP/*.clap" -type d -exec cp -r {} notarize_staging/ \;

          # Create submission zip
          ditto -c -k --keepParent notarize_staging plugins-macos-notarize.zip

          # Submit for notarization
          xcrun notarytool submit plugins-macos-notarize.zip \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait

          # Staple the notarization ticket to each plugin
          for plugin in notarize_staging/*.vst3 notarize_staging/*.component notarize_staging/*.clap; do
            if [ -d "$plugin" ]; then
              echo "Stapling: $plugin"
              xcrun stapler staple "$plugin"
            fi
          done

          cd ..

      - name: Prepare artifacts
        shell: bash
        run: |
          mkdir -p artifacts/macos/VST3 artifacts/macos/AU artifacts/macos/CLAP

          # Copy plugins (use notarized versions if available)
          if [ -d "build/notarize_staging" ]; then
            cp -r build/notarize_staging/*.vst3 artifacts/macos/VST3/ 2>/dev/null || true
            cp -r build/notarize_staging/*.component artifacts/macos/AU/ 2>/dev/null || true
            cp -r build/notarize_staging/*.clap artifacts/macos/CLAP/ 2>/dev/null || true
          else
            # Copy VST3 plugins from build/lib/, build/bin/, and build/plugins/ (handles spaces in names)
            find build/lib build/bin build/plugins -name "*.vst3" -type d -print0 2>/dev/null | while IFS= read -r -d '' vst3; do
              plugin_name=$(basename "$vst3" .vst3)
              if [ -f "$vst3/Contents/MacOS/$plugin_name" ]; then
                cp -r "$vst3" artifacts/macos/VST3/
              fi
            done

            # Copy AU plugins from build/lib/, build/bin/, and build/plugins/ (handles spaces in names)
            find build/lib build/bin build/plugins -name "*.component" -type d -print0 2>/dev/null | while IFS= read -r -d '' au; do
              plugin_name=$(basename "$au" .component)
              if [ -f "$au/Contents/MacOS/$plugin_name" ]; then
                cp -r "$au" artifacts/macos/AU/
              fi
            done

            # Copy CLAP plugins (bundle directory on macOS). clap-juce-extensions writes
            # to build/<Plugin>_artefacts/Release/CLAP/, outside the lib/bin/plugins paths.
            find build -name "*.clap" -type d -print0 2>/dev/null | while IFS= read -r -d '' clap; do
              plugin_name=$(basename "$clap" .clap)
              if [ -f "$clap/Contents/MacOS/$plugin_name" ]; then
                cp -r "$clap" artifacts/macos/CLAP/
              fi
            done
          fi

          echo "=== macOS Artifacts ==="
          find artifacts/macos -type d \( -name "*.vst3" -o -name "*.component" -o -name "*.clap" \)

      - name: Upload macOS artifacts
        uses: actions/upload-artifact@v4
        with:
          name: plugins-macos
          path: artifacts/macos/
          retention-days: 30

  # Create release when a tag is pushed
  release:
    needs: [setup, build-linux, build-linux-arm64, build-windows, build-macos]
    runs-on: ubuntu-22.04
    if: needs.setup.outputs.is_release == 'true'
    permissions:
      contents: write
    steps:
      - name: Checkout repository (for tag message)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create release archives
        env:
          RELEASE_NAME: ${{ needs.setup.outputs.release_name }}
          PLUGIN_NAME: ${{ needs.setup.outputs.plugin_name }}
        run: |
          cd artifacts

          # Extract version from tag (remove plugin prefix if present)
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION=$(echo "$TAG" | sed 's/.*-v/v/' | sed 's/^v//')

          # Create lowercase slug for zip naming (Multi-Comp -> multi-comp)
          SLUG=$(echo "$RELEASE_NAME" | tr '[:upper:]' '[:lower:]' | tr ' ' '-')

          echo "=== Creating release archives ==="
          echo "Plugin: $PLUGIN_NAME"
          echo "Release name: $RELEASE_NAME"
          echo "Slug: $SLUG"
          echo "Version: $VERSION"

          # Helper: when releasing chord-analyzer, carve out the headless LV2 bundle
          # into its own zip so Zynthian users can grab a clean, self-contained
          # download without the desktop variants. The headless bundle is the only
          # one that ships native LV2 output ControlPorts for headless host UIs.
          carve_headless() {
            local release_dir="$1"
            local out_zip="$2"
            local bundle="$release_dir/LV2/Chord Analyzer Headless.lv2"
            if [ -d "$bundle" ]; then
              local headless_dir="${release_dir}-headless"
              mkdir -p "$headless_dir/LV2"
              mv "$bundle" "$headless_dir/LV2/"
              fetch_manual "$headless_dir"
              ( cd "$headless_dir" && zip -r "../../${out_zip}" . )
            fi
          }

          # Helper: drop the per-plugin User Manual PDF into the release dir.
          # Sourced from the dusk-audio.github.io repo's main branch (issue #80).
          # A missing PDF is non-fatal so a first-release of a brand-new plugin
          # does not block. /release-plugin auto-regenerates these on every tag,
          # so the URL should be in sync with the plugin version being released.
          fetch_manual() {
            local release_dir="$1"
            local manual_url="https://raw.githubusercontent.com/dusk-audio/dusk-audio.github.io/main/assets/manuals/${SLUG}-manual.pdf"
            if curl -fL --silent --show-error "$manual_url" -o "${release_dir}/${SLUG}-manual.pdf"; then
              echo "  bundled ${SLUG}-manual.pdf into ${release_dir}/"
            else
              rm -f "${release_dir}/${SLUG}-manual.pdf"
              echo "::warning::User manual PDF not found at ${manual_url} (non-fatal)"
            fi
          }

          # Linux: Create zip with VST3/, LV2/, CLAP/ folders at root (not nested in plugins-linux/)
          # This allows users to extract directly and copy the appropriate format
          mkdir -p linux-release
          if [ -d "plugins-linux/VST3" ]; then
            cp -r plugins-linux/VST3 linux-release/
          fi
          if [ -d "plugins-linux/LV2" ]; then
            cp -r plugins-linux/LV2 linux-release/
          fi
          if [ -d "plugins-linux/CLAP" ] && [ -n "$(ls -A plugins-linux/CLAP 2>/dev/null)" ]; then
            cp -r plugins-linux/CLAP linux-release/
          fi
          carve_headless linux-release "${SLUG}-headless-linux.zip"
          fetch_manual linux-release
          cd linux-release
          zip -r "../../${SLUG}-linux.zip" .
          cd ..

          # Linux ARM64: Create zip with VST3/, LV2/, CLAP/ folders at root
          mkdir -p linux-arm64-release
          if [ -d "plugins-linux-arm64/VST3" ]; then
            cp -r plugins-linux-arm64/VST3 linux-arm64-release/
          fi
          if [ -d "plugins-linux-arm64/LV2" ]; then
            cp -r plugins-linux-arm64/LV2 linux-arm64-release/
          fi
          if [ -d "plugins-linux-arm64/CLAP" ] && [ -n "$(ls -A plugins-linux-arm64/CLAP 2>/dev/null)" ]; then
            cp -r plugins-linux-arm64/CLAP linux-arm64-release/
          fi
          carve_headless linux-arm64-release "${SLUG}-headless-linux-arm64.zip"
          fetch_manual linux-arm64-release
          cd linux-arm64-release
          zip -r "../../${SLUG}-linux-arm64.zip" .
          cd ..

          # Windows: Create zip with VST3/ and CLAP/ folders at root
          mkdir -p windows-release
          if [ -d "plugins-windows/VST3" ]; then
            cp -r plugins-windows/VST3 windows-release/
          fi
          if [ -d "plugins-windows/CLAP" ] && [ -n "$(ls -A plugins-windows/CLAP 2>/dev/null)" ]; then
            cp -r plugins-windows/CLAP windows-release/
          fi
          fetch_manual windows-release
          cd windows-release
          zip -r "../../${SLUG}-windows.zip" .
          cd ..

          # macOS: Create zip with VST3/, AU/, CLAP/ folders at root
          mkdir -p macos-release
          if [ -d "plugins-macos/VST3" ]; then
            cp -r plugins-macos/VST3 macos-release/
          fi
          if [ -d "plugins-macos/AU" ]; then
            cp -r plugins-macos/AU macos-release/
          fi
          if [ -d "plugins-macos/CLAP" ] && [ -n "$(ls -A plugins-macos/CLAP 2>/dev/null)" ]; then
            cp -r plugins-macos/CLAP macos-release/
          fi
          fetch_manual macos-release
          cd macos-release
          zip -r "../../${SLUG}-macos.zip" .
          cd ..

          # List contents for verification
          echo "=== Release Archives ==="
          ls -la ../*.zip
          echo ""
          echo "=== Linux archive contents ==="
          unzip -l "../${SLUG}-linux.zip" | head -30
          echo ""
          echo "=== Linux ARM64 archive contents ==="
          unzip -l "../${SLUG}-linux-arm64.zip" | head -30
          echo ""
          echo "=== Windows archive contents ==="
          unzip -l "../${SLUG}-windows.zip" | head -30
          echo ""
          echo "=== macOS archive contents ==="
          unzip -l "../${SLUG}-macos.zip" | head -30

      - name: Create GitHub Release
        env:
          RELEASE_NAME: ${{ needs.setup.outputs.release_name }}
          PLUGIN_NAME: ${{ needs.setup.outputs.plugin_name }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION=$(echo "$TAG" | sed 's/.*-v/v/' | sed 's/^v//')

          # Extract changelog from annotated tag message (skip the first line which is the title)
          TAG_MESSAGE=$(git tag -l --format='%(contents)' "$TAG" 2>/dev/null || echo "")
          # Remove the first line (tag title like "4K EQ v1.0.8") to avoid duplication with release title
          CHANGELOG=$(echo "$TAG_MESSAGE" | tail -n +2 | sed '/^$/d; 1{/^$/d}')

          # Build release body with changelog + installation instructions
          {
            echo "## ${PLUGIN_NAME} v${VERSION}"
            echo ""

            # Include changelog from tag if available
            if [ -n "$CHANGELOG" ]; then
              echo "$CHANGELOG"
              echo ""
            fi

            echo "### Installation"
            echo ""
            echo "Each zip contains \`VST3/\`, \`CLAP/\`, and \`LV2/\` (Linux) or \`AU/\` (macOS) folders."
            echo ""
            echo "**Linux (x86_64):**"
            echo "- VST3: Copy the \`.vst3\` folder from \`VST3/\` to \`~/.vst3/\`"
            echo "- LV2: Copy the \`.lv2\` folder from \`LV2/\` to \`~/.lv2/\`"
            echo ""
            echo "**Linux (ARM64 / Raspberry Pi):**"
            echo "- Download the \`-linux-arm64.zip\` file"
            echo "- VST3: Copy the \`.vst3\` folder from \`VST3/\` to \`~/.vst3/\`"
            echo "- LV2: Copy the \`.lv2\` folder from \`LV2/\` to \`~/.lv2/\`"
            echo "- CLAP: Copy the \`.clap\` file from \`CLAP/\` to \`~/.clap/\`"
            echo ""
            echo "**Windows:**"
            echo "- VST3: Copy the \`.vst3\` folder from \`VST3/\` to \`C:\\Program Files\\Common Files\\VST3\\\`"
            echo "- CLAP: Copy the \`.clap\` file from \`CLAP/\` to \`C:\\Program Files\\Common Files\\CLAP\\\`"
            echo ""
            echo "**macOS:**"
            echo "- VST3: Copy the \`.vst3\` bundle from \`VST3/\` to \`/Library/Audio/Plug-Ins/VST3/\` or \`~/Library/Audio/Plug-Ins/VST3/\`"
            echo "- AU: Copy the \`.component\` bundle from \`AU/\` to \`/Library/Audio/Plug-Ins/Components/\` or \`~/Library/Audio/Plug-Ins/Components/\`"
            echo "- CLAP: Copy the \`.clap\` bundle from \`CLAP/\` to \`/Library/Audio/Plug-Ins/CLAP/\` or \`~/Library/Audio/Plug-Ins/CLAP/\`"
            echo ""
            echo "> macOS CLAP bundles are not Apple-notarized. On first load you may need to right-click → Open, or remove the quarantine flag with \`xattr -dr com.apple.quarantine /path/to/Plugin.clap\`."

            # Add plugin list for "all plugins" releases
            if [ "$PLUGIN_NAME" = "All Plugins" ]; then
              echo ""
              echo "### Included Plugins"
              echo "- 4K EQ - British console EQ"
              echo "- Multi-Comp - Multi-mode compressor with multiband"
              echo "- TapeMachine - Vintage tape emulation"
              echo "- Multi-Q - Universal 8-band EQ"
              echo "- Tape Echo - Vintage tape delay"
              echo "- Convolution Reverb - IR-based reverb"
              echo "- GrooveMind - ML drum generator"
            fi

            # Chord Analyzer ships three plugin variants — give users a quick map
            if [ "$RELEASE_NAME" = "chord-analyzer" ]; then
              echo ""
              echo "### Chord Analyzer Variants"
              echo ""
              echo "The main \`chord-analyzer-*\` zips contain two variants:"
              echo "- **Chord Analyzer** — instrument variant for FL Studio / Ableton"
              echo "- **Chord Analyzer MIDI** — MIDI effect for Logic, Reaper, Bitwig, Cubase, Ardour, Carla (full UI)"
              echo ""
              echo "The separate \`chord-analyzer-headless-*\` zips contain:"
              echo "- **Chord Analyzer Headless** — native LV2 with output ControlPorts for headless hosts (Zynthian)"
              echo ""
              echo "Most users want the main zip. If you're on Zynthian or another headless LV2 host, grab the \`-headless-\` zip instead."
            fi
          } > release_body.md

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            *.zip
          generate_release_notes: true
          body_path: release_body.md
          draft: false
          prerelease: ${{ contains(github.ref, '-beta') || contains(github.ref, '-alpha') || contains(github.ref, '-rc') }}

      - name: Update Website Version
        if: ${{ needs.setup.outputs.plugin_name != 'All Plugins' }}
        env:
          RELEASE_NAME: ${{ needs.setup.outputs.release_name }}
          PLUGIN_VERSION: ${{ needs.setup.outputs.plugin_version }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          # Skip if no PAT configured or no version
          if [ -z "$GH_TOKEN" ] || [ -z "$PLUGIN_VERSION" ]; then
            echo "Skipping website update (no PAT or version)"
            exit 0
          fi

          echo "Updating website with $RELEASE_NAME version $PLUGIN_VERSION"

          # Clone website repo
          git clone https://x-access-token:${GH_TOKEN}@github.com/dusk-audio/dusk-audio.github.io.git website
          cd website

          # Update version in plugins.yml using sed
          # Format: "  version: X.X.X" under the plugin entry
          PLUGIN_SLUG="$RELEASE_NAME"

          # Update version in the plugin's markdown file (front matter)
          # This is where Jekyll reads the version from
          PLUGIN_MD="_plugins/${PLUGIN_SLUG}.md"
          if [ -f "$PLUGIN_MD" ]; then
            echo "Updating $PLUGIN_MD"
            if grep -q "^version:" "$PLUGIN_MD"; then
              sed -i "s/^version: .*/version: \"$PLUGIN_VERSION\"/" "$PLUGIN_MD"
            else
              echo "Warning: No version field found in $PLUGIN_MD"
            fi
          else
            echo "Warning: $PLUGIN_MD not found"
          fi

          # Update _data/plugins.yml using sed (preserves formatting, no yaml.dump)
          SLUG_LINE=$(grep -n "slug: ${PLUGIN_SLUG}$" _data/plugins.yml | head -1 | cut -d: -f1)
          if [ -n "$SLUG_LINE" ]; then
            # Find and update the version line within 10 lines after the slug
            sed -i "${SLUG_LINE},$((SLUG_LINE + 10))s/version: .*/version: ${PLUGIN_VERSION}/" _data/plugins.yml
            echo "Updated $PLUGIN_SLUG to version $PLUGIN_VERSION in plugins.yml"
          else
            echo "Warning: Plugin '$PLUGIN_SLUG' not found in plugins.yml"
          fi

          # Check if there are changes
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          # Commit and push
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add _data/plugins.yml
          if [ -f "_plugins/${PLUGIN_SLUG}.md" ] && ! git diff --quiet "_plugins/${PLUGIN_SLUG}.md"; then
            git add "_plugins/${PLUGIN_SLUG}.md"
          fi
          git commit -m "Update $RELEASE_NAME to v$PLUGIN_VERSION

          Automated update from release workflow."
          git push
