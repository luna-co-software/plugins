name: Build Sunset Circuits (DAF)

# Release + CI-debug workflow for the DAF-based Sunset Circuits synth.
#
# The DAF ports live outside the JUCE release build graph (build.yml), so they
# need their own matrix: this checks out dusk-audio/DAF (our fork) + dusk-audio/DAF-Widgets at
# pinned SHAs and builds plugins/sunset-circuits/daf-plugin (VST3 + CLAP + LV2
# everywhere, plus a notarizable AU on macOS with an auval pass).
#
# Triggers:
#   - push tag 'sunset-circuits-v*'  -> full release (signs+notarizes mac, cuts a
#                                       GitHub Release from the platform zips).
#   - workflow_dispatch              -> the cross-platform compile/debug loop
#                                       (unsigned; no Release job output).
#   - pull_request touching this plugin -> test-linux ONLY (the gate suite +
#                                       pluginval + clap-validator). Without it
#                                       the gates only ever ran on a release or
#                                       on a hand-fired dispatch, so a gate
#                                       regression was first seen mid-release.
#
# WHY THE CI TRIGGER IS pull_request AND NOT ALSO push-to-branch. A `paths:`
# filter under `push` applies to EVERY ref that block matches, tags included,
# and a tag push whose diff does not touch those paths can then be filtered out
# entirely -- which would silently skip a release. There is no way to give tags
# and branches different path filters inside one `push` block, so the tag
# trigger below is left exactly as it was and the path-filtered CI hangs off
# `pull_request`. Every change reaches main through a PR here, so that is the
# same coverage with none of the release risk.
on:
  push:
    tags:
      - 'sunset-circuits-v*'
  pull_request:
    paths:
      - 'plugins/sunset-circuits/**'
      - 'plugins/shared-daf/**'
      - '.github/workflows/daf-release.yml'
  workflow_dispatch:

# DAF and DAF-Widgets are pinned to permanent dusk-audio hard forks. They are
# the sole source of truth for framework changes. Keep SHAs in sync with
# daf-build.yml, daf-au-test.yml and docker/build_release.sh, and re-run the
# dispatch matrix before tagging.
env:
  DAF_SHA: 22b8282461c5847057aa31b7f210bc9e0652366e
  DAFWIDGETS_SHA: 487b092aa265458feb8320308a1fa36ca0901a5b
  PLUGIN_DIR: plugins/sunset-circuits/daf-plugin
  SLUG: sunset-circuits

jobs:
  # Parse the release version out of the tag; empty on workflow_dispatch, which
  # lets the DAF CMakeLists fall back to SUNSETCIRCUITS_DEFAULT_VERSION (1.0.0).
  setup:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      version: ${{ steps.v.outputs.version }}
      is_release: ${{ steps.v.outputs.is_release }}
      prerelease: ${{ steps.v.outputs.prerelease }}
    steps:
      # Needed by the tag<->CMake version guard below, which reads the plugin's
      # CMakeLists.txt.
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Check DAF artefact naming standard
        run: ./tests/check_daf_plugin_names.sh

      - name: Parse tag
        id: v
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION=""
          IS_RELEASE="false"
          PRERELEASE="false"
          if [[ "$GITHUB_REF" == refs/tags/sunset-circuits-v* ]]; then
            RAW="${TAG#sunset-circuits-v}"
            # Accept X.Y.Z with an optional prerelease suffix (-alpha, -beta.2,
            # -rc1, ...). CMake's project(VERSION) only takes the numeric core,
            # so export that; the validated suffix drives the prerelease flag in
            # the release job. A malformed tag fails HERE, before any build or
            # publish step, instead of silently shipping the default version.
            if [[ "$RAW" =~ ^([0-9]+\.[0-9]+\.[0-9]+)(-[A-Za-z0-9.]+)?$ ]]; then
              IS_RELEASE="true"
              VERSION="${BASH_REMATCH[1]}"
              if [[ -n "${BASH_REMATCH[2]}" ]]; then PRERELEASE="true"; fi
              # Guard: the tag version must equal the CMake version (the numeric
              # core, so an -rc/-beta suffix is ignored), else the release ships
              # binaries whose embedded version disagrees with the tag. This
              # plugin declares the version in SUNSETCIRCUITS_DEFAULT_VERSION and
              # only passes the resolved variable to project(), so read the set()
              # -- grepping the project() line would find no literal. || true so a
              # missing/renamed variable reports the message below instead of
              # dying silently on errexit.
              CMAKE_VERSION=$(grep -m1 -oE 'SUNSETCIRCUITS_DEFAULT_VERSION[[:space:]]+"[0-9]+\.[0-9]+\.[0-9]+"' \
                "$PLUGIN_DIR/CMakeLists.txt" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' || true)
              if [ -z "$CMAKE_VERSION" ] || [ "$VERSION" != "$CMAKE_VERSION" ]; then
                echo "::error::Tag '$TAG' version '$VERSION' != CMake SUNSETCIRCUITS_DEFAULT_VERSION '${CMAKE_VERSION:-<not found>}' in $PLUGIN_DIR/CMakeLists.txt"
                exit 1
              fi
            else
              echo "::error::malformed release tag '$TAG' (want sunset-circuits-vX.Y.Z or sunset-circuits-vX.Y.Z-<suffix>)"
              exit 1
            fi
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "is_release=$IS_RELEASE" >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"
          echo "Version: ${VERSION:-<default 1.0.0>}  is_release: $IS_RELEASE  prerelease: $PRERELEASE"

  build-linux-x64:
    needs: setup
    # RELEASE/DISPATCH ONLY. On a pull_request this job is skipped: CI runs the
    # gate suite (test-linux) and nothing else, because the four platform builds
    # exist to produce release artifacts and cost ~40 min each. github.ref is
    # refs/pull/<n>/merge on a PR, so this is false there and true for both a
    # tag push and a manual dispatch.
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 40
    container:
      image: ubuntu:20.04  # glibc 2.31, matching the JUCE fleet (build.yml)
    env:
      PLUGIN_VERSION_OVERRIDE: ${{ needs.setup.outputs.version }}
    steps:
      - name: Install base tools
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Inline because checkout cannot run until git is installed and the
          # shared script is not on disk yet. Stock Ubuntu repositories only.
          # The timeout guards stalls, retries guard hard failures, and the
          # dpkg repair handles interrupted transactions before the next try.
          bootstrap() {
            timeout 90 apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=30 && \
            timeout 150 apt-get install -y --no-install-recommends -o Acquire::Retries=3 \
              git ca-certificates
          }
          for i in 1 2; do
            bootstrap && break
            if [ "$i" = "2" ]; then
              echo "::error::apt bootstrap unavailable after 2 attempts"
              exit 1
            fi
            echo "::warning::bootstrap attempt $i failed or timed out; retrying in 10s"
            timeout 45 dpkg --configure -a || true
            sleep 10
          done

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install Linux apt dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
          # Worst case, counting the 45s dpkg repair that precedes each retry:
          # bootstrap 2x(90+150)+45+10 = 8.9m; plain packages 2x(60+120)+45+5
          # = 6.8m; PPA retry = 2x90+10 = 3.2m; post-source update 2x60+45+5
          # = 2.8m; final packages = 6.8m. Total apt ceiling is 28.6m,
          # leaving over 11m in this 40m job.
          APT_ATTEMPTS: 2
          APT_UPDATE_TIMEOUT: 60
          APT_INSTALL_TIMEOUT: 120
          APT_RETRY_SLEEP: 5
        run: |
          # The PPA command is the only apt operation the shared script cannot own.
          .github/scripts/apt_install.sh software-properties-common wget gpg
          for i in 1 2; do
            timeout 90 add-apt-repository -y ppa:ubuntu-toolchain-r/test && break
            [ "$i" = "2" ] && exit 1
            echo "PPA attempt 1 failed; retrying in 10s"
            sleep 10
          done
          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ focal main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
          .github/scripts/apt_install.sh --update-only
          # libxtst-dev: every Linux job configures the plugin, and the CMake that
          # registers the editor drag guard requires XTest. The guard fails the
          # configure rather than skipping itself, so the package belongs in every
          # Linux dep list, not only the ones that go on to run ctest.
          .github/scripts/apt_install.sh \
            gcc-11 g++-11 cmake ninja-build pkg-config zip unzip \
            libgl1-mesa-dev libglu1-mesa-dev mesa-common-dev \
            libx11-dev libxext-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxtst-dev libdbus-1-dev
          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 100
          update-alternatives --install /usr/bin/cc  cc  /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/c++ c++ /usr/bin/g++-11 100
          g++ --version
          cmake --version
          ninja --version

      - name: Checkout DAF
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF
          ref: ${{ env.DAF_SHA }}
          path: DAF
          submodules: recursive
          persist-credentials: false

      - name: Checkout DAF-Widgets
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF-Widgets
          ref: ${{ env.DAFWIDGETS_SHA }}
          path: DAF-Widgets
          submodules: recursive
          persist-credentials: false

      - name: Configure
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDUSK_DAF_INSTALL_LOCAL=OFF \
            -DDAF_PATH="${GITHUB_WORKSPACE}/DAF" \
            -DDAFWIDGETS_PATH="${GITHUB_WORKSPACE}/DAF-Widgets"

      - name: Build (VST3 + CLAP + LV2)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake --build build --target sunset-circuits-vst3 sunset-circuits-clap sunset-circuits-lv2 -j"$(nproc)"

      - name: Package
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          mkdir -p out/VST3 out/CLAP out/LV2
          cp -r build/bin/sunset-circuits.vst3 out/VST3/
          cp    build/bin/sunset-circuits.clap out/CLAP/
          cp -r build/bin/sunset-circuits.lv2  out/LV2/
          test -f "out/VST3/sunset-circuits.vst3/Contents/x86_64-linux/sunset-circuits.so"
          test -f "out/LV2/sunset-circuits.lv2/manifest.ttl"
          # Bundle the published user manual (non-fatal if absent), same as
          # daf-build.yml does for the "-2" plugins.
          manual_url="https://raw.githubusercontent.com/dusk-audio/dusk-audio.github.io/main/assets/manuals/${SLUG}-manual.pdf"
          curl -fL --silent --show-error "$manual_url" -o "out/${SLUG}-manual.pdf" \
            || { rm -f "out/${SLUG}-manual.pdf"; echo "::warning::User manual PDF not found at ${manual_url} (non-fatal)"; }
          ( cd out && zip -r "../${SLUG}-linux.zip" . )
          echo "=== contents ==="; unzip -l "${SLUG}-linux.zip"

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SLUG }}-linux
          path: ${{ env.PLUGIN_DIR }}/${{ env.SLUG }}-linux.zip
          retention-days: 30

  build-linux-arm64:
    needs: setup
    # RELEASE/DISPATCH ONLY. On a pull_request this job is skipped: CI runs the
    # gate suite (test-linux) and nothing else, because the four platform builds
    # exist to produce release artifacts and cost ~40 min each. github.ref is
    # refs/pull/<n>/merge on a PR, so this is false there and true for both a
    # tag push and a manual dispatch.
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
    timeout-minutes: 40
    container:
      image: ubuntu:22.04  # glibc 2.35, matching build.yml's arm job
    env:
      PLUGIN_VERSION_OVERRIDE: ${{ needs.setup.outputs.version }}
    steps:
      - name: Install base tools
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Inline because checkout cannot run until git is installed and the
          # shared script is not on disk yet. Stock Ubuntu repositories only.
          # The timeout guards stalls, retries guard hard failures, and the
          # dpkg repair handles interrupted transactions before the next try.
          bootstrap() {
            timeout 90 apt-get update -o Acquire::Retries=3 -o Acquire::http::Timeout=30 && \
            timeout 150 apt-get install -y --no-install-recommends -o Acquire::Retries=3 \
              git ca-certificates
          }
          for i in 1 2; do
            bootstrap && break
            if [ "$i" = "2" ]; then
              echo "::error::apt bootstrap unavailable after 2 attempts"
              exit 1
            fi
            echo "::warning::bootstrap attempt $i failed or timed out; retrying in 10s"
            timeout 45 dpkg --configure -a || true
            sleep 10
          done

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install GCC 11 toolchain
        env:
          DEBIAN_FRONTEND: noninteractive
          # Worst case, counting the 45s dpkg repair that precedes each retry:
          # bootstrap 2x(90+150)+45+10 = 8.9m; plain packages 2x(60+120)+45+5
          # = 6.8m; post-source update 2x60+45+5 = 2.8m; final packages =
          # 6.8m. Total apt ceiling is 25.4m, leaving
          # over 17m in this 40m job for checkout, configuration, and build.
          APT_ATTEMPTS: 2
          APT_UPDATE_TIMEOUT: 60
          APT_INSTALL_TIMEOUT: 120
          APT_RETRY_SLEEP: 5
        run: |
          # Ubuntu 22.04 ships gcc-11 natively — no PPA needed.
          .github/scripts/apt_install.sh wget gpg
          wget -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | tee /usr/share/keyrings/kitware-archive-keyring.gpg >/dev/null
          echo 'deb [signed-by=/usr/share/keyrings/kitware-archive-keyring.gpg] https://apt.kitware.com/ubuntu/ jammy main' | tee /etc/apt/sources.list.d/kitware.list >/dev/null
          .github/scripts/apt_install.sh --update-only
          # libxtst-dev: every Linux job configures the plugin, and the CMake that
          # registers the editor drag guard requires XTest. The guard fails the
          # configure rather than skipping itself, so the package belongs in every
          # Linux dep list, not only the ones that go on to run ctest.
          .github/scripts/apt_install.sh \
            gcc-11 g++-11 cmake ninja-build pkg-config zip unzip \
            libgl1-mesa-dev libglu1-mesa-dev mesa-common-dev \
            libx11-dev libxext-dev libxrandr-dev libxinerama-dev \
            libxcursor-dev libxtst-dev libdbus-1-dev
          update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-11 100
          update-alternatives --install /usr/bin/cc  cc  /usr/bin/gcc-11 100
          update-alternatives --install /usr/bin/c++ c++ /usr/bin/g++-11 100
          g++ --version
          cmake --version
          ninja --version

      - name: Checkout DAF
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF
          ref: ${{ env.DAF_SHA }}
          path: DAF
          submodules: recursive
          persist-credentials: false

      - name: Checkout DAF-Widgets
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF-Widgets
          ref: ${{ env.DAFWIDGETS_SHA }}
          path: DAF-Widgets
          submodules: recursive
          persist-credentials: false

      - name: Configure
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDUSK_DAF_INSTALL_LOCAL=OFF \
            -DDAF_PATH="${GITHUB_WORKSPACE}/DAF" \
            -DDAFWIDGETS_PATH="${GITHUB_WORKSPACE}/DAF-Widgets"

      - name: Build (VST3 + CLAP + LV2)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake --build build --target sunset-circuits-vst3 sunset-circuits-clap sunset-circuits-lv2 -j"$(nproc)"

      - name: Package
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          mkdir -p out/VST3 out/CLAP out/LV2
          cp -r build/bin/sunset-circuits.vst3 out/VST3/
          cp    build/bin/sunset-circuits.clap out/CLAP/
          cp -r build/bin/sunset-circuits.lv2  out/LV2/
          test -f "out/VST3/sunset-circuits.vst3/Contents/aarch64-linux/sunset-circuits.so"
          test -f "out/LV2/sunset-circuits.lv2/manifest.ttl"
          # Bundle the published user manual (non-fatal if absent).
          manual_url="https://raw.githubusercontent.com/dusk-audio/dusk-audio.github.io/main/assets/manuals/${SLUG}-manual.pdf"
          curl -fL --silent --show-error "$manual_url" -o "out/${SLUG}-manual.pdf" \
            || { rm -f "out/${SLUG}-manual.pdf"; echo "::warning::User manual PDF not found at ${manual_url} (non-fatal)"; }
          ( cd out && zip -r "../${SLUG}-linux-arm64.zip" . )
          echo "=== contents ==="; unzip -l "${SLUG}-linux-arm64.zip"

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SLUG }}-linux-arm64
          path: ${{ env.PLUGIN_DIR }}/${{ env.SLUG }}-linux-arm64.zip
          retention-days: 30

  build-windows:
    needs: setup
    # RELEASE/DISPATCH ONLY. On a pull_request this job is skipped: CI runs the
    # gate suite (test-linux) and nothing else, because the four platform builds
    # exist to produce release artifacts and cost ~40 min each. github.ref is
    # refs/pull/<n>/merge on a PR, so this is false there and true for both a
    # tag push and a manual dispatch.
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    runs-on: windows-2022
    permissions:
      contents: read
    timeout-minutes: 60
    env:
      PLUGIN_VERSION_OVERRIDE: ${{ needs.setup.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Checkout DAF
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF
          ref: ${{ env.DAF_SHA }}
          path: DAF
          submodules: recursive
          persist-credentials: false

      - name: Checkout DAF-Widgets
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF-Widgets
          ref: ${{ env.DAFWIDGETS_SHA }}
          path: DAF-Widgets
          submodules: recursive
          persist-credentials: false

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Pre-fetch Khronos GL headers for DAF
        shell: pwsh
        run: |
          # DAF's MSVC path downloads GL/glext.h + KHR/khrplatform.h from
          # khronos.org at configure time; that registry URL has moved and the
          # fetch silently yields a broken header (NanoVG.cpp then fails with
          # missing PFNGL*PROC typedefs). Pre-populate from the canonical Khronos
          # GitHub registry so DAF's `if(NOT EXISTS)` skips the broken download.
          # Pinned to immutable registry commits + SHA-256 verified, so a moved
          # branch or tampered/truncated download fails loudly here rather than
          # compiling (or shipping) against an unvetted header.
          $gl = "${{ github.workspace }}/DAF/khronos/GL"
          $khr = "${{ github.workspace }}/DAF/khronos/KHR"
          New-Item -ItemType Directory -Force -Path $gl, $khr | Out-Null
          $glRev  = "9d527dbc81bb76e35ba284fe385ed8a5ddb90cbc"  # KhronosGroup/OpenGL-Registry
          $khrRev = "3d7796b3721d93976b6bfe536aa97bbc4bce8667"  # KhronosGroup/EGL-Registry
          Invoke-WebRequest -Uri "https://raw.githubusercontent.com/KhronosGroup/OpenGL-Registry/$glRev/api/GL/glext.h" -OutFile "$gl/glext.h"
          Invoke-WebRequest -Uri "https://raw.githubusercontent.com/KhronosGroup/EGL-Registry/$khrRev/api/KHR/khrplatform.h" -OutFile "$khr/khrplatform.h"
          $expected = @{
            "$gl/glext.h"        = "B381ABF98F9705E4B3EFACB6850D2A86982B3039F66CCE88FF7F0350F46F04BF"
            "$khr/khrplatform.h" = "7B1E01AAA7AD8F6FC34B5C7BDF79EBF5189BB09E2C4D2E79FC5D350623D11E83"
          }
          foreach ($f in $expected.Keys) {
            $h = (Get-FileHash -Algorithm SHA256 $f).Hash
            if ($h -ne $expected[$f]) { Write-Error "checksum mismatch for ${f}: $h"; exit 1 }
          }
          Write-Host "glext.h size: $((Get-Item "$gl/glext.h").Length) bytes (checksums OK)"

      - name: Patch DAF-Widgets for MSVC
        shell: pwsh
        run: |
          # DAF-Widgets' DearImGui.cpp used the GNU 'Elvis operator' (a ?: b), which
          # MSVC does not accept (C2059). The fork corrected it, so at the current
          # pin the rewrite below matches nothing and the assertion carries the
          # step; both halves stay because the pin has already moved back and forth
          # across that correction once, and a future bump could reintroduce the
          # construct. Deterministic either way (fresh pinned checkout each run).
          #
          # The assertion is the part that matters: it fails by name instead of as
          # a C2059 buried a hundred lines into cl's output. DearImGui.cpp is the
          # only DAF-Widgets TU our plugins compile (DUSK_DAF_UI_SOURCES in
          # shared-daf/cmake/DuskDafPlugin.cmake).
          $f = "${{ github.workspace }}/DAF-Widgets/opengl/DearImGui.cpp"
          $c = [System.IO.File]::ReadAllText($f)
          $c = $c.Replace('self->getWidth() ?: 640 * scaleFactor',  'self->getWidth() ? self->getWidth() : 640 * scaleFactor')
          $c = $c.Replace('self->getHeight() ?: 480 * scaleFactor', 'self->getHeight() ? self->getHeight() : 480 * scaleFactor')
          [System.IO.File]::WriteAllText($f, $c)
          if ($c.Contains('?:')) {
            Write-Error "DAF-Widgets DearImGui.cpp still contains the GNU Elvis operator (a ?: b) after patching, which MSVC rejects. The rewrite above no longer matches the pinned source."
            exit 1
          }
          Write-Host "Patched DearImGui.cpp"

      - name: Configure
        working-directory: ${{ env.PLUGIN_DIR }}
        shell: pwsh
        run: |
          cmake -B build -G Ninja `
            -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl `
            -DCMAKE_BUILD_TYPE=Release `
            -DDUSK_DAF_INSTALL_LOCAL=OFF `
            -DDAF_PATH="${{ github.workspace }}/DAF" `
            -DDAFWIDGETS_PATH="${{ github.workspace }}/DAF-Widgets"

      - name: Build (VST3 + CLAP + LV2)
        working-directory: ${{ env.PLUGIN_DIR }}
        shell: pwsh
        run: |
          cmake --build build --target sunset-circuits-vst3 sunset-circuits-clap sunset-circuits-lv2 -j $env:NUMBER_OF_PROCESSORS

      - name: Package
        working-directory: ${{ env.PLUGIN_DIR }}
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path out/VST3, out/CLAP, out/LV2 | Out-Null
          Copy-Item -Recurse build/bin/sunset-circuits.vst3 out/VST3/
          Copy-Item          build/bin/sunset-circuits.clap out/CLAP/
          Copy-Item -Recurse build/bin/sunset-circuits.lv2  out/LV2/
          # Bundle the published user manual (non-fatal if absent).
          $manualUrl = "https://raw.githubusercontent.com/dusk-audio/dusk-audio.github.io/main/assets/manuals/$env:SLUG-manual.pdf"
          try { Invoke-WebRequest -Uri $manualUrl -OutFile "out/$env:SLUG-manual.pdf" }
          catch { Remove-Item -Force -ErrorAction SilentlyContinue "out/$env:SLUG-manual.pdf"; Write-Host "::warning::User manual PDF not found at $manualUrl (non-fatal)" }
          Compress-Archive -Path out/* -DestinationPath "$env:SLUG-windows.zip" -Force
          Write-Host "=== contents ==="
          Get-ChildItem -Recurse out | Select-Object FullName

      - name: Install pluginval
        shell: pwsh
        run: |
          # pluginval ships a ready-to-run Windows binary in its GitHub releases;
          # trivially installable, so we run a headless VST3 validation here.
          # SHA-256 pinned: release assets are practically immutable but the tag
          # is not cryptographically so — verify before executing anything.
          Invoke-WebRequest -Uri "https://github.com/Tracktion/pluginval/releases/download/v1.0.3/pluginval_Windows.zip" -OutFile pluginval.zip
          $h = (Get-FileHash -Algorithm SHA256 pluginval.zip).Hash
          if ($h -ne "4D83BDE21AEBAF4D52EC0CA6D9F0C7119AD4181288EB82DA103FADE31A8523AD") {
            Write-Error "pluginval_Windows.zip checksum mismatch: $h"
            exit 1
          }
          Expand-Archive pluginval.zip -DestinationPath pluginval -Force
          Get-ChildItem -Recurse pluginval -Filter pluginval.exe | Select-Object -First 1

      - name: Validate VST3 (pluginval, headless)
        working-directory: ${{ env.PLUGIN_DIR }}
        shell: pwsh
        run: |
          $pv = Get-ChildItem -Recurse "$env:GITHUB_WORKSPACE/pluginval" -Filter pluginval.exe | Select-Object -First 1
          if (-not $pv) { Write-Error "pluginval.exe not found after install"; exit 1 }
          $vst3 = (Resolve-Path "build/bin/sunset-circuits.vst3").Path
          # --skip-gui-tests: no display on the runner. pluginval crashes on DAF's
          # headless plugin teardown (the known DAF editor-lifecycle issue,
          # docs/daf-migration/00-OVERVIEW.md landmine 9) AFTER the tests pass, so
          # a non-zero exit is tolerated ONLY when the log shows tests actually
          # ran and none failed. A missing executable, a plugin that fails to
          # load, or a real test failure all fail the job — we must never release
          # an unloadable VST3. This is the lighter of the two pluginval runs:
          # the test-linux job below runs strictness 8 on Linux, and the real-DAW
          # matrix in Phase 5 (QA checklist Part 2) remains the final word. The
          # shipping Linux jobs above build and package only.
          # --timeout-ms bounds a hung test rather than letting it ride the job
          # timeout (landmine 9), matching the Linux job.
          $log = & $pv.FullName --strictness-level 5 --skip-gui-tests --timeout-ms 270000 --validate $vst3 2>&1 | Out-String
          Write-Host $log
          if ($LASTEXITCODE -eq 0) {
            Write-Host "pluginval: SUCCESS"
            exit 0
          }
          if ($log -match 'Starting test' -and $log -notmatch 'FAILED') {
            Write-Host "::warning::pluginval exited non-zero after tests ran clean (DAF headless teardown, known issue, non-fatal)"
            exit 0
          }
          Write-Error "pluginval reported a real failure (load error or failed test)"
          exit 1

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SLUG }}-windows
          path: ${{ env.PLUGIN_DIR }}/${{ env.SLUG }}-windows.zip
          retention-days: 30

  build-macos:
    needs: setup
    # RELEASE/DISPATCH ONLY. On a pull_request this job is skipped: CI runs the
    # gate suite (test-linux) and nothing else, because the four platform builds
    # exist to produce release artifacts and cost ~40 min each. github.ref is
    # refs/pull/<n>/merge on a PR, so this is false there and true for both a
    # tag push and a manual dispatch.
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    runs-on: macos-14
    permissions:
      contents: read
    timeout-minutes: 60
    env:
      PLUGIN_VERSION_OVERRIDE: ${{ needs.setup.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Checkout DAF
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF
          ref: ${{ env.DAF_SHA }}
          path: DAF
          submodules: recursive
          persist-credentials: false

      - name: Checkout DAF-Widgets
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF-Widgets
          ref: ${{ env.DAFWIDGETS_SHA }}
          path: DAF-Widgets
          submodules: recursive
          persist-credentials: false

      - name: Install Ninja
        run: brew install ninja

      - name: Configure (Universal Binary)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDUSK_DAF_INSTALL_LOCAL=OFF \
            -DCMAKE_OSX_ARCHITECTURES="arm64;x86_64" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET="10.15" \
            -DDAF_PATH="${{ github.workspace }}/DAF" \
            -DDAFWIDGETS_PATH="${{ github.workspace }}/DAF-Widgets"

      - name: Build (VST3 + CLAP + LV2 + AU)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake --build build --target sunset-circuits-vst3 sunset-circuits-clap sunset-circuits-lv2 sunset-circuits-au -j"$(sysctl -n hw.ncpu)"

      - name: Verify universal binary
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          BIN="build/bin/sunset-circuits.vst3/Contents/MacOS/sunset-circuits"
          lipo -info "$BIN"
          lipo -info "$BIN" | grep -q "arm64" && lipo -info "$BIN" | grep -q "x86_64" \
            && echo "Universal confirmed" || { echo "::error::VST3 not universal"; exit 1; }

      - name: Install AU + auval
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          COMP=$(find build -name '*.component' -type d | head -1)
          if [ -z "$COMP" ]; then echo "::error::No AU component built"; exit 1; fi
          echo "AU: $COMP"
          plutil -p "$COMP/Contents/Info.plist" || cat "$COMP/Contents/Info.plist"
          mkdir -p "$HOME/Library/Audio/Plug-Ins/Components"
          cp -R "$COMP" "$HOME/Library/Audio/Plug-Ins/Components/"
          killall -9 AudioComponentRegistrar 2>/dev/null || true
          echo "--- registered Dusk instruments ---"
          auval -a | grep -i -E "dusk|DsSC" || echo "(not listed yet; validating by code)"
          # Sunset Circuits is an instrument -> AU type 'aumu'; subtype DsSC, manufacturer Dusk.
          auval -v aumu DsSC Dusk

      - name: Import Code Signing Certificate
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' && startsWith(github.ref, 'refs/tags/') }}
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          echo $MACOS_CERTIFICATE | base64 --decode > certificate.p12
          security import certificate.p12 -P "$MACOS_CERTIFICATE_PWD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH
          rm certificate.p12

      - name: Code Sign Plugins
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' && startsWith(github.ref, 'refs/tags/') }}
        working-directory: ${{ env.PLUGIN_DIR }}
        env:
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        run: |
          for plugin in build/bin/sunset-circuits.vst3 build/bin/sunset-circuits.component build/bin/sunset-circuits.clap; do
            if [ -d "$plugin" ]; then
              echo "Signing: $plugin"
              codesign --force --deep --sign "$MACOS_SIGNING_IDENTITY" --options runtime --timestamp "$plugin"
              codesign --verify --verbose "$plugin"
            fi
          done

      - name: Notarize Plugins
        if: ${{ vars.MACOS_SIGNING_ENABLED == 'true' && startsWith(github.ref, 'refs/tags/') }}
        working-directory: ${{ env.PLUGIN_DIR }}
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          mkdir -p notarize_staging
          for plugin in build/bin/sunset-circuits.vst3 build/bin/sunset-circuits.component build/bin/sunset-circuits.clap; do
            [ -d "$plugin" ] && cp -R "$plugin" notarize_staging/
          done
          ditto -c -k --keepParent notarize_staging plugins-macos-notarize.zip
          xcrun notarytool submit plugins-macos-notarize.zip \
            --apple-id "$APPLE_ID" --password "$APPLE_APP_PASSWORD" --team-id "$APPLE_TEAM_ID" --wait
          for plugin in notarize_staging/*.vst3 notarize_staging/*.component notarize_staging/*.clap; do
            [ -d "$plugin" ] && xcrun stapler staple "$plugin"
          done

      - name: Package
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          mkdir -p out/VST3 out/CLAP out/LV2 out/AU
          # Prefer notarized+stapled bundles when present.
          if [ -d notarize_staging ]; then
            cp -r notarize_staging/sunset-circuits.vst3      out/VST3/ 2>/dev/null || true
            cp -r notarize_staging/sunset-circuits.clap      out/CLAP/ 2>/dev/null || true
            cp -r notarize_staging/sunset-circuits.component out/AU/   2>/dev/null || true
          fi
          [ -e out/VST3/sunset-circuits.vst3 ]      || cp -r build/bin/sunset-circuits.vst3      out/VST3/
          [ -e out/CLAP/sunset-circuits.clap ]      || cp -r build/bin/sunset-circuits.clap      out/CLAP/
          [ -e out/AU/sunset-circuits.component ]   || cp -r build/bin/sunset-circuits.component out/AU/
          cp -r build/bin/sunset-circuits.lv2 out/LV2/
          # Bundle the published user manual (non-fatal if absent).
          manual_url="https://raw.githubusercontent.com/dusk-audio/dusk-audio.github.io/main/assets/manuals/${SLUG}-manual.pdf"
          curl -fL --silent --show-error "$manual_url" -o "out/${SLUG}-manual.pdf" \
            || { rm -f "out/${SLUG}-manual.pdf"; echo "::warning::User manual PDF not found at ${manual_url} (non-fatal)"; }
          ( cd out && zip -r "../${SLUG}-macos.zip" . )
          echo "=== contents ==="; unzip -l "${SLUG}-macos.zip"

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SLUG }}-macos
          path: ${{ env.PLUGIN_DIR }}/${{ env.SLUG }}-macos.zip
          retention-days: 30

  # Gate suite + Linux validation. Runs on BOTH triggers (tag push and
  # workflow_dispatch), and the release job needs it, so a red gate blocks the
  # publish rather than being noticed afterwards.
  #
  # Deliberately NOT in the ubuntu:20.04 container the shipping linux jobs use:
  # this job produces no artifact, so it has no glibc floor to respect, and the
  # plain runner gives it a modern python, liblilv and pluginval without fighting
  # focal's package set. The binaries it builds are for testing only.
  test-linux:
    needs: setup
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    # Generous: the shipping linux job budgets 40 minutes for the build alone,
    # and this one adds the gate suite and pluginval on top of a build of
    # comparable size.
    timeout-minutes: 75
    env:
      PLUGIN_VERSION_OVERRIDE: ${{ needs.setup.outputs.version }}
    steps:
      # Checkout moved ahead of the dependency install so the shared apt script
      # is on disk when that step runs. Safe here and only here: this job runs on
      # a hosted runner, which already has git, unlike the bare containers above
      # where the first apt call is what makes checkout possible at all.
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Install build + test deps
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # libxtst-dev: the UI drag guard synthesises pointer input with XTest to
          # prove a knob still turns. Without it that gate is not built at all.
          .github/scripts/apt_install.sh \
            cmake ninja-build pkg-config unzip \
            libgl1-mesa-dev libglu1-mesa-dev mesa-common-dev \
            libx11-dev libxext-dev libxrandr-dev libxinerama-dev libxcursor-dev \
            libxtst-dev \
            libdbus-1-dev \
            liblilv-dev \
            xvfb
          # liblilv-dev enables the suite's lv2_smoke target; without it that one
          # gate self-skips and the LV2 host path would go unvalidated, so fail
          # here rather than let the suite quietly skip it.
          pkg-config --modversion lilv-0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python gate deps
        run: |
          # Pinned to minor series: this job gates the release, and the gates
          # compare measured numbers against fixed thresholds, so a numpy major
          # bump must not silently move an answer. Bump deliberately, then
          # re-run the suite.
          #
          # scipy is REQUIRED, not optional. fx_alias_gate imports
          # scipy.signal.resample_poly and self-skips its resampling sections
          # when the import fails -- so without scipy the "fx_alias_gate" step
          # printed a skip notice while the step comment claimed it ran, and the
          # effects-chain aliasing report was empty on every CI run.
          python3 -m pip install 'numpy==1.26.*' 'soundfile==0.12.*' 'scipy==1.11.*'

      - name: Checkout DAF
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF
          ref: ${{ env.DAF_SHA }}
          path: DAF
          submodules: recursive
          persist-credentials: false

      - name: Checkout DAF-Widgets
        uses: actions/checkout@v4
        with:
          repository: dusk-audio/DAF-Widgets
          ref: ${{ env.DAFWIDGETS_SHA }}
          path: DAF-Widgets
          submodules: recursive
          persist-credentials: false

      - name: Configure
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DDUSK_DAF_INSTALL_LOCAL=OFF \
            -DDAF_PATH="${GITHUB_WORKSPACE}/DAF" \
            -DDAFWIDGETS_PATH="${GITHUB_WORKSPACE}/DAF-Widgets"

      - name: Build (VST3 + CLAP + LV2)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          # The suite's lv2_smoke gate hosts build/bin/sunset-circuits.lv2, so the
          # LV2 bundle must exist before run_all.sh; pluginval below needs the
          # VST3 and clap-validator needs the CLAP.
          cmake --build build --target \
            sunset-circuits-vst3 sunset-circuits-clap sunset-circuits-lv2 -j"$(nproc)"

      - name: Core gate suite
        run: |
          # Full suite: every pass/fail gate, the fm and acid sub-suites, the
          # 54-preset audit, lv2_smoke, and the cpu_bench sanity bar. The
          # fx_alias 54-preset sweep stays opt-in (FX_ALIAS_FULL=1) -- it only
          # changes answer when the factory presets change and is by far the
          # slowest item in the suite.
          plugins/sunset-circuits/core/tests/run_all.sh

      - name: Editor drag guard (ctest)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          # Opens the plugin's real editor and drags a knob, including across a
          # focus change. Every other gate here skips the GUI, which is how a
          # DAF-Widgets regression that killed knob dragging in every DAF plugin
          # reached four releases. xvfb-run because this one genuinely needs a
          # display; the harness fails rather than skipping when DISPLAY is unset.
          cmake --build build --config Release -j"$(nproc)"
          xvfb-run -a ctest --test-dir build --output-on-failure

      - name: Install pluginval
        run: |
          # SHA-256 pinned: the release asset is practically immutable but the tag
          # is not cryptographically so -- verify before executing anything.
          # Staged in RUNNER_TEMP so nothing lands in the checkout.
          cd "$RUNNER_TEMP"
          curl -fsSL -o pluginval.zip \
            https://github.com/Tracktion/pluginval/releases/download/v1.0.3/pluginval_Linux.zip
          echo "69adc87d6bbb2f7224a038d8f8c5e737ea5c906ea153752b8a546b6125e0564f  pluginval.zip" \
            | sha256sum -c -
          unzip -o pluginval.zip -d pluginval
          # Locate the binary rather than assuming the archive layout (the
          # Windows job does the same); a layout change should fail loudly.
          PV="$(find pluginval -type f -name pluginval | head -n1)"
          if [ -z "$PV" ]; then
            echo "::error::pluginval binary not found after install"
            exit 1
          fi
          chmod +x "$PV"
          echo "PLUGINVAL=$RUNNER_TEMP/$PV" >> "$GITHUB_ENV"
          "$RUNNER_TEMP/$PV" --version || true

      - name: Validate VST3 (pluginval, headless)
        working-directory: ${{ env.PLUGIN_DIR }}
        run: |
          VST3="$(cd build/bin && pwd)/sunset-circuits.vst3"
          test -d "$VST3"
          # --skip-gui-tests is mandatory, not an optimisation: pluginval's editor
          # tests segfault headless for every DAF plugin (host-side XEmbed issue,
          # docs/daf-migration/00-OVERVIEW.md landmine 9). Strictness 8 is the bar
          # the QA checklist calls authoritative for the 223-parameter state
          # round-trip; the Windows job runs 5 on top of this.
          #
          # Run under xvfb-run even though --skip-gui-tests means no editor is
          # opened: that is the pattern 00-OVERVIEW.md prescribes and the one the
          # local QA runs use, and this job gates the release -- not the place to
          # rely on pluginval's untested no-X path.
          #
          # Output is tee'd, not captured: a hung pluginval hitting the job
          # timeout with everything still in a shell variable would leave zero
          # diagnostic output. The shell-level `timeout` bounds it well inside
          # the job timeout so a hang fails with the partial log intact.
          #
          # Same tolerated-teardown rule as the Windows job: DAF can exit non-zero
          # tearing down after a clean run, and that is accepted ONLY when the log
          # shows tests actually ran and none failed. A plugin that fails to load
          # or a real failed test still fails the job.
          set +e
          timeout 900 xvfb-run -a "$PLUGINVAL" \
            --strictness-level 8 --skip-gui-tests --timeout-ms 270000 \
            --validate "$VST3" 2>&1 | tee pluginval.log
          STATUS=${PIPESTATUS[0]}
          set -e
          if [ "$STATUS" -eq 0 ]; then
            echo "pluginval: SUCCESS"
            exit 0
          fi
          if [ "$STATUS" -eq 124 ]; then
            echo "::error::pluginval timed out after 900s (partial log above)"
            exit 1
          fi
          if grep -q 'Starting test' pluginval.log && ! grep -q 'FAILED' pluginval.log; then
            echo "::warning::pluginval exited non-zero after tests ran clean (DAF headless teardown, known issue, non-fatal)"
            exit 0
          fi
          echo "::error::pluginval reported a real failure (load error or failed test)"
          exit 1

      - name: Validate CLAP (clap-validator gate)
        working-directory: ${{ env.PLUGIN_DIR }}
        env:
          # STRICT: any failing clap-validator test fails the release, exactly as
          # on daf-build.yml. Sunset Circuits runs clean, so there is nothing to
          # be advisory about; if that ever changes, fix the plugin rather than
          # loosening this (NEVER a per-test allowlist -- see the script).
          SUITE_STRICT: "1"
        run: |
          # This calls the SHARED gate rather than an inline classifier.
          #
          # The inline version this replaces downloaded its own pinned
          # clap-validator and classified the JSON itself: it printed warnings as
          # advisory and failed only on `failed` tests. That misses the
          # clap_host_latency::changed regression, which clap-validator 0.4.1
          # reports as a WARNING and does NOT count as a failed test, so a DAF
          # latency-contract regression would have shipped through the release
          # path while daf-build.yml hard-failed the same plugin. One place now
          # defines what makes a CLAP acceptable -- including the pinned
          # validator version and its SHA-256, which the script resolves itself.
          #
          # xvfb: clap-validator drives the plugin GUI, which needs an X display.
          set -o pipefail
          CLAP="$(cd build/bin && pwd)/sunset-circuits.clap"
          test -f "$CLAP"
          xvfb-run -a python3 "${GITHUB_WORKSPACE}/.github/scripts/daf_clap_validate.py" \
            "$CLAP" 2>&1 | tee clap-validator.log

      - name: Upload validation logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.SLUG }}-validation-linux-logs
          path: |
            ${{ env.PLUGIN_DIR }}/pluginval.log
            ${{ env.PLUGIN_DIR }}/clap-validator.log
          retention-days: 14
          if-no-files-found: ignore

  # Assemble the GitHub Release from the four platform zips (tags only).
  # test-linux is in needs: so a red gate suite blocks the publish.
  release:
    needs: [setup, test-linux, build-linux-x64, build-linux-arm64, build-windows, build-macos]
    runs-on: ubuntu-24.04
    if: needs.setup.outputs.is_release == 'true'
    permissions:
      contents: write
    steps:
      - name: Checkout repository (for tag message)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Assemble release notes
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#sunset-circuits-v}"
          TAG_MESSAGE=$(git tag -l --format='%(contents)' "$TAG" 2>/dev/null || echo "")
          CHANGELOG=$(echo "$TAG_MESSAGE" | tail -n +2 | sed '/^$/d; 1{/^$/d}')
          {
            echo "## Sunset Circuits v${VERSION}"
            echo ""
            if [ -n "$CHANGELOG" ]; then echo "$CHANGELOG"; echo ""; fi
            echo "### Installation"
            echo ""
            echo "Each zip contains \`VST3/\`, \`CLAP/\`, \`LV2/\` and (macOS) \`AU/\` folders."
            echo ""
            echo "**Linux (x86_64 / ARM64):** copy \`.vst3\` to \`~/.vst3/\`, \`.lv2\` to \`~/.lv2/\`, \`.clap\` to \`~/.clap/\`."
            echo ""
            echo "**Windows:** copy \`.vst3\` to \`C:\\Program Files\\Common Files\\VST3\\\`, \`.clap\` to \`C:\\Program Files\\Common Files\\CLAP\\\`."
            echo ""
            echo "**macOS:** copy \`.vst3\` to \`~/Library/Audio/Plug-Ins/VST3/\`, \`.component\` to \`~/Library/Audio/Plug-Ins/Components/\`, \`.clap\` to \`~/Library/Audio/Plug-Ins/CLAP/\`."
          } > release_body.md
          echo "=== staged zips ==="; find artifacts -name '*.zip'

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*.zip
          # generate_release_notes is OFF: with no prior tag in this workflow's
          # lineage GitHub walks the ENTIRE repo PR history into the body, which
          # blew the 125k-char release-body limit on v1.0.0 (422, three retries,
          # job failed). body_path carries the changelog; auto-notes add nothing
          # we want here.
          generate_release_notes: false
          body_path: release_body.md
          draft: false
          # Derived from the validated tag suffix in the setup job (any
          # -<suffix> after X.Y.Z), not from a substring scan of the ref.
          prerelease: ${{ needs.setup.outputs.prerelease == 'true' }}
